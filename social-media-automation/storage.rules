rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Media is private during migration. The backend will issue controlled
    // URLs when Buffer needs to fetch an approved asset.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
