rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // The first migration phase is backend-only. Cloud Run uses its service
    // account through the Admin SDK, which bypasses these client rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
